# Frontend environment variables (Vite)
# Backend API URL used by the frontend to reach the Node/Express API
# Use trailing slash for consistency
VITE_API_URL:https://medical-be-93i1.onrender.com

# Example: Vite will expose env vars that start with VITE_
# VITE_RAZORPAY_KEY="your_key_here"

# For local overrides, create a `.env.local` file and add your secrets there.
